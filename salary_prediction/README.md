#IF PROGRAM NOT START (TIPS FOR DEBIAN DISTRIBUTION LIKE UBUNTU)

sudo easy_install pip 
sudo easy_install --upgrade six 
#the CPU version of the binaries to Pypi 
sudo pip install --ignore-installed tensorflow
